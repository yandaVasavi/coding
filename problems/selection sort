class Solution:
    def selectionSort(self, nums):
        n=len(nums)
        for i in range(0,n-1):
            min=i
            for j in range(i+1,n):
                if(nums[j]<nums[min]):
                    min=j
            nums[min],nums[i]=nums[i],nums[min]
        return nums
